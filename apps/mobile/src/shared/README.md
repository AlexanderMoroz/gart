# shared

FSD layer: UI kit, API client (built from `@gart/contract`), utils.
No imports from entities/features/app.

The template's `src/components`, `src/constants`, and `src/hooks` example code
folds into this layer when the real screens land.
